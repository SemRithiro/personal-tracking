import App from '../../package.json';

export const DATE_OPTION = 'DD MMM YYYY';

export const THRESHOLD_TIME = 11;

export const APP_SECRET = 'secret';
export const APP_NAME = App.name;
export const APP_VERSION = App.version;
